#A vector is a one-dimensional array. We can create a vector with all the basic data type we learnt before. 
#The simplest way to build a vector in R, is to use the c command.

vec_1 <- c(1, 10, 49)
vec_1


vec_chr <- c("a", "b", "c")
vec_chr

vec_bol <- c(TRUE , FALSE , TRUE)
vec_bol

vec_2 <- c(3,7,22)
vec_2
 
vec_sum <- vec_1 + vec_2
vec_1
vec_2
vec_sum

slice_vect <- c(1,35,4,335,34,67,2,45,22,68,35,64,26,5)
slice_vect
slice_vect[2:7]

c(5:10)

