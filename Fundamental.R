#we can check the type of variable with class() function
y <- 'R is Fantastic'
class(y)
z <- '43'
class(z)
a <- TRUE
class(a)
v <- "Variables store values and are an important component in programming,
      especially for a data scientist. A variable can store a number, an object,
      a statistical result, vector, dataset, a model prediction basically
      anything R outputs. We can use that variable later simply by calling the name of the variable."
class(v)
